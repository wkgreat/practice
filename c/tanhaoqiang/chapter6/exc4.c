#include<stdio.h>
// not complete
int mine(void);

int main(void)
{
	printf("\n");return 0;
}

int mine(viod)
{
	double a[10],b[50];
	double n;
	for(int i=0;i<10;i++)
	  a[i] = i;
	printf("Please enter a number>>>");
	scanf("%d", &n);
	for(i=0;i<10)
}
